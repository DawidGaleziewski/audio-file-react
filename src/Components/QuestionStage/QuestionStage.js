import React from 'react';
import Section from '../Section/Section';

const QuestionStage = () => {
  return (
    <Section headerText="Answer the question">
      <p>
        Play the sound. Select which one from below is correct source of sound.
      </p>
    </Section>
  );
};

export default QuestionStage;
